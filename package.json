{
  "name": "github-issue-search-app",
  "version": "1.0.0",
  "description": "Simple React interactive app for searching github issues",
  "scripts": {
    "start-dev": "npm run clean && parcel src/index.html",
    "start-prod": "npm run clean && parcel build src/index.html && http-server -p 8080 ./dist -o http://localhost:8080/index.html",
    "clean": "rimraf dist"
  },
  "author": "Marjan Mitev",
  "license": "UNLICENSED",
  "dependencies": {
    "axios": "^0.19.0",
    "flexboxgrid": "^6.3.1",
    "http-server": "^0.11.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-timeago": "^4.4.0",
    "rimraf": "^3.0.0"
  },
  "devDependencies": {
    "@types/react": "^16.9.2",
    "@types/react-dom": "^16.8.5",
    "parcel-bundler": "^1.12.3",
    "sass": "^1.22.10",
    "typescript": "^3.5.3"
  }
}
